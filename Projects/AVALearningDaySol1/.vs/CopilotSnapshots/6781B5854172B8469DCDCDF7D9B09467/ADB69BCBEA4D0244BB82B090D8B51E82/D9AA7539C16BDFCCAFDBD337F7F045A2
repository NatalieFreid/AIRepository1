/// <summary>
/// UI builder for the AVAInventoryTest SysOperation contract.
/// Uses the automatic UI builder to create dialog fields from the data contract
/// and customizes the dialog presentation for this operation.
/// </summary>
/// <remarks>
/// The automatic builder inspects the data contract and generates the appropriate
/// dialog fields. Override build() only to perform UI adjustments such as setting
/// captions, adding help text or rearranging fields.
/// </remarks>
/// <see>SysOperationAutomaticUIBuilder</see>
class AVAInventoryTestUIBuilder extends SysOperationAutomaticUIBuilder
{
    /// <summary>
    /// Build the dialog/UI for the data contract.
    /// We call the automatic builder then adjust the dialog caption.
    /// </summary>
    /// <returns>Dialog instance built by the automatic UI builder.</returns>
    public Dialog build()
    {
        Dialog dialog = super.build();
        if (dialog)
        {
            dialog.caption("AVAInventoryTest - Calculate open inventory");
        }

        return dialog;
    }
}