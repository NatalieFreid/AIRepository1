<?xml version="1.0" encoding="utf-8"?>
<AxClass xmlns:i="http://www.w3.org/2001/XMLSchema-instance">
	<Name>AVADAILookupHelper_cls_Extension</Name>
	<SourceCode>
		<Declaration><![CDATA[
/// <summary>
/// CoC extension of class AVADAILookupHelper
/// </summary>
[ExtensionOf(classStr(AVADAILookupHelper))]
final class AVADAILookupHelper_cls_Extension
{
}
]]></Declaration>
		<Methods>
			<Method>
				<Name>reportName</Name>
				<Source><![CDATA[
    /// <summary>
    /// Lookup report names.
    /// </summary>
    /// <param name = "_lookupCtrl">Control for which to perform the lookup</param>
    /// <param name = "_reportType">Report type</param>
    public static void reportName(FormStringControl _lookupCtrl, AVADAIReportType _reportType)
    {
        next reportName(_lookupCtrl, _reportType);

        if (isConfigurationKeyEnabled(configurationkeynum(AVADAIAttachment)))
        {
            if (_reportType == AVADAIReportType::Attachment)
            {
                AVADAILookUpTmp reportLookup;
            
                var tables = Microsoft.Dynamics.Ax.Xpp.MetadataSupport::TableNames();
                while (tables.MoveNext())
                {
                    reportLookup.clear();
                    reportLookup.LookupItemName = tables.Current;
                    reportLookup.insert();
                }

                Query lookupQuery = new Query();
                QueryBuildDataSource qbds = lookupQuery.addDataSource(tablenum(AVADAILookUpTmp));
                qbds.addOrderByField(fieldNum(AVADAILookUpTmp,LookupItemname));
        
                SysTableLookup sysTableLookup = SysTableLookup::newParameters(tablenum(AVADAILookUpTmp), _lookupCtrl);
                sysTableLookup.addLookupfield(fieldnum(AVADAILookUpTmp, LookupItemName));
                sysTableLookup.parmQuery(lookupQuery);
                sysTableLookup.parmTmpBuffer(reportLookup);
                sysTableLookup.performFormLookup();
            }
        }
    }

]]></Source>
			</Method>
			<Method>
				<Name>reportDesignName</Name>
				<Source><![CDATA[
    /// <summary>
    /// Lookup report designs.
    /// </summary>
    /// <param name = "_lookupCtrl">Control for which to perform the lookup</param>
    /// <param name = "_reportName">Name of the report of which the designs must be shown</param>
    public static void reportDesignName(FormStringControl _lookupCtrl, ReportName _reportName)
    {
        ReportName reportName = _reportName;

        Microsoft.Dynamics.AX.Metadata.MetaModel.AxTable table = Microsoft.Dynamics.Ax.Xpp.MetadataSupport::GetTable(_reportName);
        if (table != null)
        {
            AVADAILookUpTmp reportDesignLookup;
            reportDesignLookup.clear();
            reportDesignLookup.LookupItemName = AVADAIDocuActionDMSFileURL::classDescription();
            reportDesignLookup.insert();
        
            // Create a custom Query
            Query lookupQuery = new Query();
            QueryBuildDataSource qbds = lookupQuery.addDataSource(tablenum(AVADAILookUpTmp));
            qbds.addOrderByField(fieldNum(AVADAILookUpTmp,LookupItemname));
            SysTableLookup sysTableLookup = SysTableLookup::newParameters(tablenum(AVADAILookUpTmp), _lookupCtrl);

            // Display the Name field in the lookup form.
            sysTableLookup.addLookupfield(fieldnum(AVADAILookUpTmp, LookupItemName));
            sysTableLookup.parmQuery(lookupQuery);
            sysTableLookup.parmTmpBuffer(reportDesignLookup);
            sysTableLookup.performFormLookup();
            
            // Skip standard execution
            reportName = '';
        }

        next reportDesignName(_lookupCtrl, reportName);
    }

]]></Source>
			</Method>
		</Methods>
	</SourceCode>
</AxClass>