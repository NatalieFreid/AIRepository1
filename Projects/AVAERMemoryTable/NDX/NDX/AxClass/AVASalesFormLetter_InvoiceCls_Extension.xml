<?xml version="1.0" encoding="utf-8"?>
<AxClass xmlns:i="http://www.w3.org/2001/XMLSchema-instance">
	<Name>AVASalesFormLetter_InvoiceCls_Extension</Name>
	<SourceCode>
		<Declaration><![CDATA[
/// <summary>
///     The <c>AVASalesFormLetter_InvoiceCls_Extension</c> class is the extension for <c>SalesFormLetter_Invoice</c>
/// </summary>
[ExtensionOf(classStr(SalesFormLetter_Invoice))]
final class AVASalesFormLetter_InvoiceCls_Extension
{
}
]]></Declaration>
		<Methods>
			<Method>
				<Name>validate</Name>
				<Source><![CDATA[
    /// <summary>
    /// Validates whether it is all right to continue.
    /// </summary>
    /// <param name="_calledFrom">
    /// Identifies the object from which this method is called. The default value is null.
    /// </param>
    /// <returns>
    /// true if it is all right to continue; otherwise, false.
    /// </returns>
    /// <remarks>
    /// The validate method is used for validating if it is all right to close the sales posting form when
    /// you post sales orders.
    /// </remarks>
    boolean validate(Object _calledFrom)
    {
        boolean     ok;
        boolean     locCheckOk;
        //SalesTable  locSalesTable;
        SalesParmTable  salesParmTableLocal;
        SalesTable      salesTableLocal;

        Counter         recordCount         = 0;
        boolean         interCompanyFound   = false;
        SalesParmUpdate salesParmUpdate = this.salesParmUpdate();
        
        ok = next validate(_calledFrom);

      /*  locSalesTable = this.salesTable();
        if(locSalesTable.RecId)
        {
            ok = ok && locSalesTable.avaCheckMarkupFreightInformationBeforeIPost();

            locCheckOk = locSalesTable.avaCheckFreightInformationBeforeIPost(false);
            if(!locCheckOk)
            {
                checkFailed(strFmt("@NDX:NXASalesOrderP1FreightCostMissung", locSalesTable.SalesId));
            }
            ok = ok && locCheckOk;
        }        */

       

       // if (salesParmUpdate.SumBy == AccountOrder::Order)
       // {
            while select RecId, SalesId from salesParmTableLocal
                where salesParmTableLocal.ParmId == salesParmUpdate.ParmId
                    join   salesTableLocal
                    where salesTableLocal.SalesId == salesParmTableLocal.SalesId
            {
                ok = ok && salesTableLocal.avaCheckMarkupFreightInformationBeforeIPost();

                locCheckOk = ok  &&  salesTableLocal.avaCheckFreightInformationBeforeIPost(false);
                if(!locCheckOk)
                {
                    checkFailed(strFmt("@NDX:NXASalesOrderP1FreightCostMissung", salesTableLocal.SalesId));
                }
                ok = ok && locCheckOk;
            }
       // }
        
        return ok;
    }

]]></Source>
			</Method>
		</Methods>
	</SourceCode>
</AxClass>