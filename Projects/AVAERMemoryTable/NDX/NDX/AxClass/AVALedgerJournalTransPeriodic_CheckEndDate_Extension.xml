<?xml version="1.0" encoding="utf-8"?>
<AxClass xmlns:i="http://www.w3.org/2001/XMLSchema-instance">
	<Name>AVALedgerJournalTransPeriodic_CheckEndDate_Extension</Name>
	<SourceCode>
		<Declaration><![CDATA[
/// <summary>
/// AVALedgerJournalTransPeriodic_CheckEndDate_Extension
/// </summary>
[ExtensionOf(formControlStr(LedgerJournalTransPeriodic, AVA_CheckDate))]
final class AVALedgerJournalTransPeriodic_CheckEndDate_Extension
{
 
}
]]></Declaration>
		<Methods>
			<Method>
				<Name>clicked</Name>
				<Source><![CDATA[
    /// <summary>
    /// clicked
    /// </summary>
    public void clicked()
    {
        boolean recordFound = false;
        FormButtonControl formButtonControl = any2Object(this) as FormButtonControl;
        FormDataSource formDatasource = formButtonControl.formRun().dataSource(tableStr(LedgerJournalTrans));
        LedgerJournalTrans ledgerJournalTransCur = formDatasource.cursor();
        LedgerJournalTrans ledgerJournalTrans;
        DimensionAttributeValueCombination dimensionAttributeValueCombination;
       
        next clicked();

        while select Voucher,Txt, AVAEndDate, LedgerDimension, JournalNum from ledgerJournalTrans where ledgerJournalTrans.JournalNum == ledgerJournalTransCur.JournalNum
        {
          
            if(ledgerJournalTrans.AVAEndDate <= DateTimeUtil::getToday(DateTimeUtil::getUserPreferredTimeZone()) && ledgerJournalTrans.AVAEndDate != dateNull())
            {
                recordFound = true;
                select firstonly DisplayValue, RecId from dimensionAttributeValueCombination where dimensionAttributeValueCombination.RecId == ledgerJournalTrans.LedgerDimension;
                Info(strFmt("@NDX:NXAEndDateCheckInfo", ledgerJournalTrans.Voucher, dimensionAttributeValueCombination.DisplayValue, ledgerJournalTrans.Txt));
            }
        }
        
        if(recordFound == false)
        {
            info("@NDX:NXAEndDateCheckSuccess");
        }
    }

]]></Source>
			</Method>
		</Methods>
	</SourceCode>
</AxClass>