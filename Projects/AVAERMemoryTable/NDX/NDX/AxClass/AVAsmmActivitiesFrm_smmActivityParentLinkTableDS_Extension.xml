<?xml version="1.0" encoding="utf-8"?>
<AxClass xmlns:i="http://www.w3.org/2001/XMLSchema-instance">
	<Name>AVAsmmActivitiesFrm_smmActivityParentLinkTableDS_Extension</Name>
	<SourceCode>
		<Declaration><![CDATA[
/// <summary>
/// extension of FDS smmActivityParentLinkTable
/// </summary>
[ExtensionOf(FormDatasourceStr(smmActivities, smmActivityParentLinkTable))]
final class AVAsmmActivitiesFrm_smmActivityParentLinkTableDS_Extension
{
}
]]></Declaration>
		<Methods>
			<Method>
				<Name>initValue</Name>
				<Source><![CDATA[
    public void initValue()
    {
        //FormDataSource              smmActivities_ds;
        //smmActivities               locsmmActivities;
        //FormDataSource              smmActivityParentLinkTable_ds;
        //smmActivityParentLinkTable  locsmmActivityParentLinkTable;

        next initValue();

        //smmActivities_ds                = element.dataSource(1);
        //locsmmActivities                = smmActivities_ds.cursor();
        //locsmmActivities                = element.smmActivities;
        //smmActivityParentLinkTable_ds   = this;
        //locsmmActivityParentLinkTable   = smmActivityParentLinkTable_ds.cursor();
        //locsmmActivityParentLinkTable   = element.smmActivityParentLinkTable;

        /*if(locsmmActivities.RecId && locsmmActivities.Category == smmActivityCategory::Action)
        {
            locsmmActivityParentLinkTable.ParentType = smmActivityParentType::BuildingProject;
        } */
    }

]]></Source>
			</Method>
			<Method>
				<Name>linkedId</Name>
				<Source><![CDATA[
    /// <summary>
    /// AVA
    /// </summary>
    /// <param name = "_set">boolean</param>
    /// <param name = "_smmActivityParentLinkTable">smmActivityParentLinkTable</param>
    /// <param name = "_linkId">smmActivityParentLinkId</param>
    /// <returns>smmActivityParentLinkId</returns>
    edit smmActivityParentLinkId linkedId(  boolean                     _set,
                                            smmActivityParentLinkTable  _smmActivityParentLinkTable,
                                            smmActivityParentLinkId     _linkId)
    {
        FormDataSource  locFormDataSource;
        //smmActivities   locsmmActivityParentLinkTable;

        smmActivityParentLinkId retVal;

        locFormDataSource = this;
        //locsmmActivityParentLinkTable = locFormDataSource.cursor();
        // does not work in this scope

        retVal = next linkedId( _set,
                                _smmActivityParentLinkTable,
                                _linkId);

        return retVal;
    
        /*
        edit smmActivityParentLinkId linkedId(boolean _set, smmActivityParentLinkTable _smmActivityParentLinkTable, smmActivityParentLinkId _linkId)
        {
            Common common;

            FieldId fieldId = _smmActivityParentLinkTable.type2FieldId();
            DictTable dictTable = new DictTable(_smmActivityParentLinkTable.RefTableId);

            if (dictTable)
            {
                common = dictTable.makeRecord();
            }

            if (_set)
            {
                if (_linkId)
                {
                    select firstonly common
                    where common.(fieldId) == _linkId;

                    if (common)
                    {
                        _smmActivityParentLinkTable.RefRecId = common.RecId;
                        refreshLinks = true;
                    }
                    else
                    {
                        _smmActivityParentLinkTable.RefRecId = 0;
                    }
                }
                else
                {
                    _smmActivityParentLinkTable.RefRecId = 0;
                }
            }
            else if (_smmActivityParentLinkTable.RefRecId)
            {
                select firstonly common
                where common.RecId == _smmActivityParentLinkTable.RefRecId;
            }

            if (common)
            {
                return common.(fieldId);
            }
            else
            {
                return '';
            }
        }
    
        */
    
    }

]]></Source>
			</Method>
		</Methods>
	</SourceCode>
</AxClass>