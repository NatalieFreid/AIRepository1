<?xml version="1.0" encoding="utf-8"?>
<AxClass xmlns:i="http://www.w3.org/2001/XMLSchema-instance">
	<Name>AVAERReportCleanupService</Name>
	<SourceCode>
		<Declaration><![CDATA[
/// <summary>
/// Service class for AVAER Report Memory Cleanup batch job
/// Cleans up Report field content in AVAERReportToMemory table for specified date ranges
/// </summary>
internal final class AVAERReportCleanupService extends SysOperationServiceBase
{
    
}
]]></Declaration>
		<Methods>
			<Method>
				<Name>cleanupReportData</Name>
				<Source><![CDATA[
    /// <summary>
    /// Main cleanup method that processes the report data cleanup
    /// </summary>
    /// <param name = "_contract">The data contract containing query parameters</param>
    [SysEntryPointAttribute(true)]
    public void cleanupReportData(AVAERReportCleanupContract _contract)
    {
        QueryRun                queryRun;
        CustQuotationTable      custQuotationTable;
        AVAERReportToMemory     avaerReportToMemory;
        SysOperationProgress    progress;
        int                     recordsProcessed = 0;
        int                     recordsCleaned = 0;
        int                     totalRecords = 0;
        Query                   query;
        
        try
        {
            // Validate parameters
            if (!this.validateParameters(_contract))
            {
                return;
            }

            // Initialize progress indicator
            progress = new SysOperationProgress();
            progress.setCaption("AVAER Report Cleanup Progress");
            
            // Get the query from contract
            query = _contract.parmQuery();
            
            if (!query)
            {
                throw error("Query not specified");
            }
            
            // Create QueryRun from Query
            queryRun = new QueryRun(query);
            
            // Count total records for progress tracking
            while (queryRun.next())
            {
                totalRecords++;
            }
            
            // Reset query for actual processing
            queryRun.reset();
            progress.setTotal(totalRecords);
            
            info(strFmt("AVAER Report cleanup started. Processing %1 records.", totalRecords));
            
            // Process each CustQuotationTable record
            while (queryRun.next())
            {
                custQuotationTable = queryRun.getNo(1);
                
                if (custQuotationTable.RecId)
                {
                    recordsProcessed++;
                    progress.setText(strFmt("Processing quotation: %1", custQuotationTable.QuotationId));
                    progress.setCount(recordsProcessed, 1);
                    
                    // Find and clean related AVAERReportToMemory records
                    recordsCleaned += this.cleanupMemoryTableRecords(custQuotationTable);
                }
            }
            
            // Final status message
            info(strFmt("AVAER Report cleanup completed. Processed %1 quotations, cleaned %2 memory records.", recordsProcessed, recordsCleaned));
        }
        catch (Exception::Error)
        {
            error("Error occurred during AVAER Report cleanup");
            throw;
        }
    }
]]></Source>
			</Method>
			<Method>
				<Name>cleanupMemoryTableRecords</Name>
				<Source><![CDATA[
    /// <summary>
    /// Cleans up AVAERReportToMemory records for a specific CustQuotationTable record
    /// </summary>
    /// <param name = "_custQuotationTable">The CustQuotationTable record to process</param>
    /// <returns>Number of records cleaned</returns>
    private int cleanupMemoryTableRecords(CustQuotationTable _custQuotationTable)
    {
        AVAERReportToMemory     avaerReportToMemory;
        int                     cleanedCount = 0;
        
        try
        {
            ttsbegin;
            
            // Find all AVAERReportToMemory records related to this quotation
            while select forupdate avaerReportToMemory
                where avaerReportToMemory.RefTableId == tableNum(CustQuotationTable) &&
                      avaerReportToMemory.RefRecId == _custQuotationTable.RecId &&
                      avaerReportToMemory.Report != conNull()
            {
                // Clear the Report field content
                avaerReportToMemory.Report = conNull();
                avaerReportToMemory.update();
                cleanedCount++;
            }
            
            ttscommit;
            
            if (cleanedCount > 0)
            {
                info(strFmt("Cleaned %1 memory records for quotation %2", cleanedCount, _custQuotationTable.QuotationId));
            }
        }
        catch (Exception::Error)
        {
            ttsabort;
            error(strFmt("Error cleaning memory records for quotation %1", _custQuotationTable.QuotationId));
            throw;
        }
        
        return cleanedCount;
    }
]]></Source>
			</Method>
			<Method>
				<Name>validateParameters</Name>
				<Source><![CDATA[
    /// <summary>
    /// Validates the parameters before processing
    /// </summary>
    /// <param name = "_contract">The data contract to validate</param>
    /// <returns>True if parameters are valid</returns>
    private boolean validateParameters(AVAERReportCleanupContract _contract)
    {
        Query query;
        
        if (!_contract)
        {
            error("Data contract is not specified");
            return false;
        }
        
        query = _contract.parmQuery();
        
        if (!query)
        {
            error("Query is not specified in the contract");
            return false;
        }
        
        return true;
    }
]]></Source>
			</Method>
			<Method>
				<Name>cleanupMemoryTableRecordsBulk</Name>
				<Source><![CDATA[
    /// <summary>
    /// Alternative bulk cleanup method for better performance on large datasets
    /// Uses delete_from for bulk operations instead of record-by-record updates
    /// </summary>
    /// <param name = "_custQuotationTable">The CustQuotationTable record to process</param>
    /// <returns>Number of records cleaned</returns>
    private int cleanupMemoryTableRecordsBulk(CustQuotationTable _custQuotationTable)
    {
        AVAERReportToMemory     avaerReportToMemory;
        int                     cleanedCount = 0;
        
        try
        {
            ttsbegin;
            
            // First, count the records that will be updated
            select count(RecId) from avaerReportToMemory
                where avaerReportToMemory.RefTableId == tableNum(CustQuotationTable) &&
                      avaerReportToMemory.RefRecId == _custQuotationTable.RecId &&
                      avaerReportToMemory.Report != conNull();
            
            cleanedCount = avaerReportToMemory.RecId;
            
            // Bulk update to clear Report field
            update_recordset avaerReportToMemory
                setting Report = conNull()
                where avaerReportToMemory.RefTableId == tableNum(CustQuotationTable) &&
                      avaerReportToMemory.RefRecId == _custQuotationTable.RecId &&
                      avaerReportToMemory.Report != conNull();
            
            ttscommit;
            
            if (cleanedCount > 0)
            {
                info(strFmt("Bulk cleaned %1 memory records for quotation %2", cleanedCount, _custQuotationTable.QuotationId));
            }
        }
        catch (Exception::Error)
        {
            ttsabort;
            error(strFmt("Error in bulk cleanup for quotation %1", _custQuotationTable.QuotationId));
            throw;
        }
        
        return cleanedCount;
    }
]]></Source>
			</Method>
		</Methods>
	</SourceCode>
	<Label>AVAER Report Cleanup Service</Label>
	<HelpText>Service class for cleaning up AVAER report data stored in memory</HelpText>
	<RunOn>Server</RunOn>
</AxClass>